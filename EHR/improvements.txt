Improvements:
- remove leading and trailing spaces from user input
- forgot password
- how for the backend to log in with the  user ccrediential 
- eyes image to see your password
- logout route 
- redesign the webpage 




 # mycursor.execute("SELECT COUNT(*) as total_patients FROM patients WHERE doctor_id = %s", (doctor_id,))
        # total_patients = mycursor.fetchone()['total_patients']
        # print(total_patients)
        # mycursor.execute("""
        #     SELECT ehr_data.*, patients.first_name, patients.last_name 
        #     FROM ehr_data 
        #     JOIN patients ON ehr_data.patient_id = patients.id 
        #     WHERE ehr_data.doctor_id = %s
        #     ORDER BY ehr_data.date DESC 
        #     LIMIT 5
        # """, (doctor_id,))
        # recent_ehr = mycursor.fetchall()



        mycursor.execute("""
            SELECT ehr_data.*, patients.first_name, patients.last_name 
            FROM ehr_data
            JOIN patients ON ehr_data.patient_id = patients.id
            WHERE ehr_data.patient_id = %s
        """, (patient_id,))